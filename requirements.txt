# Runtime dependencies for the Biofeedback pipeline.
# Versions pinned to what is installed and known-working in the env/ venv.
#
# Install:  env\Scripts\activate  &&  pip install -r requirements.txt

# --- LSL networking ---
# Lab Streaming Layer client. Used by the streamer, acquisition, output,
# dashboard, and session_control modules for inter-process messaging.
pylsl==1.16.2

# --- Numerical core ---
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3

# --- Signal processing (R-peak detection, HRV metrics) ---
# Primary R-peak detector for the adaptive path (mock / real_plux), and
# the full sliding-window derivation chain for the NeuroKit path
# (mock2 / real_plux2). Both code paths import this.
neurokit2==0.2.13

# --- Dashboard GUI + plotting ---
PyQt5==5.15.11
pyqtgraph==0.14.0

# --- Offline review (session_review.py) ---
matplotlib==3.10.9
# Pillow is matplotlib's image backend; needed for --save *.png / *.jpg.
pillow==12.2.0

# --- XLSX sidecar with frozen header + autofit columns ---
# session_manager writes a .xlsx mirror of samples.csv at session end so
# the operator can scroll through the data with the header row pinned.
openpyxl==3.1.5

# --- Optional: reading the docx specs in the repo root ---
# Not imported by the runtime pipeline, but useful for anyone who wants
# to load shayans_biofeedback_math_pipeline.docx / walkthrough.docx
# programmatically. Safe to remove if disk space matters.
python-docx==1.2.0
